from google.colab import drive
import logging
import multiprocessing
from gensim.corpora import WikiCorpus, MmCorpus
from gensim.models.word2vec import Word2Vec
import numpy as np

logging.basicConfig(format='%(asctime)s : %(levelname)s : %(message)s', level=logging.INFO)

drive.mount('/content/drive', force_remount=True)

file = 'drive/My Drive/file/ruwiki-latest-pages-articles.xml.bz2'

wiki = WikiCorpus(file, lemmatize=False)

sentences = (wiki.get_texts())

params = {'size': 200, 'window': 10, 'min_count': 10,'workers': max(1, multiprocessing.cpu_count() -1), 'sample': 1E-3,}

word2vec = Word2Vec(new, **params)
